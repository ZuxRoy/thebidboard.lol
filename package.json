{
  "name": "thebidboard.lol",
  "private": true,
  "workspaces": [
    "app",
    "api"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:app": "turbo run dev --filter=app",
    "dev:api": "turbo run dev --filter=api"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=20"
  }
}
